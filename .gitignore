# Xcode
build/
DerivedData/
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Downloaded CI logs dropped into the repo for debugging
logs_*/


# Claude session notes (never committed)
.claude-notes/

# Skills-sync hook state (per-machine, not project content)
.claude/.skills-sync-marker
